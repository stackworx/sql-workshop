import {setup, tearDown} from './util';

import {Course} from '../models';

beforeEach(async () => setup());
afterAll(async () => tearDown());

test('insert using objection with nesting', async () => {
  // given
  // when
  // then
});
